/**
 * The timer is used in each of the channels to control the sound frequency.
 */
export class TimerUnit {
    timerCycle: number;
    timerPeriod: number;
    reset(): void;
    set timer(arg: number);
    /** @type {number} */
    get timer(): number;
}
export default TimerUnit;
//# sourceMappingURL=TimerUnit.d.ts.map